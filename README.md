# 決密槍戰 Online FPS

## 本機
1. `npm install`
2. `npm start`
3. 開啟 `http://localhost:3000`

## Render
- Runtime: Node
- Build Command: `npm install`
- Start Command: `npm start`

注意：目前 Three.js 與兩把武器模型由 CDN / GitHub raw 載入；部署時需要網路可用。
